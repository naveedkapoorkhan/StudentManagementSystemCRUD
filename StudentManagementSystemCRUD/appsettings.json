{
    "ConnectionStrings": {
        "DefaultConnection": "Server=.;Database=StudentManagementSystemCRUD;Trusted_Connection=True;TrustServerCertificate=True;"
    },

    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },

    "AllowedHosts": "*"
}

//"ConnectionStrings" → section to store DB connections.

//"DefaultConnection" → the name we will use in Program.cs.

//"Server=.;" → . means your local SQL Server (you can replace with localhost or DESKTOP-XXXX\\SQLEXPRESS if using SQL Express).

//"Database=StudentManagementSystemCRUD;" → your DB will be created with this name.

//"Trusted_Connection=True;" → Windows Authentication login.

//"TrustServerCertificate=True;" → avoids SSL certificate issues.